<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material">
        <!-- Customize your theme here. -->
        <item name="android:colorAccent">@color/material_red_400</item>
        <item name="android:textColorPrimary">@color/material_red_300_155</item>
    </style>

    <style name="AppThemeInit" parent="android:Theme.Material">
        <!-- Customize your theme here. -->
        <item name="android:colorAccent">@color/material_red_400</item>
        <item name="android:textColorPrimary">@color/material_red_300_155</item>
    </style>

    <style name="splash" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

</resources>